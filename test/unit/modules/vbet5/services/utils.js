describe('Utils service', function(){

  beforeEach(module('vbet5'));

  it('should create "phones" model with 3 phones', inject(function($service) {
//     var scope = {},
//         service = $service('Utils', {$scope:scope});

    expect(3).toBe(3);
  }));

});